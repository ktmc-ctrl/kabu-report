# kabu — 株式相談ツール

日本株の運用を、**相談・記録・調査・レポート**の4つでまわす作業場。
このリポジトリは GitHub Pages で公開されている(https://ktmc-ctrl.github.io/kabu-report/)。
何をどこに書くかを間違えると、ポジションが世界中から見えるので、まずそこから。

## 最重要: 2層のプライバシー構造

| 層 | 場所 | 内容 |
|---|---|---|
| **私的(git に乗らない)** | `private/` `CLAUDE.local.md` | ポジション・取得単価・出口・運用ルール・日記 |
| **公開(友達と共有)** | リポジトリ直下の `*.html` `data/` | 決算分析のみ |

`private/` と `CLAUDE.local.md` は `.gitignore` 済み。**この2つの中身を公開側のファイルへ
コピーしない。** 判断を規律に任せず `python3 tool/kabu.py check` が機械的に検査する。
公開に関わる操作(`portal` / `publish`)は必ずこの検査を通す。

公開レポートは**である調**で、主語は会社。「買い」「売り」「狙い目」等の推奨語は使わない。
末尾には必ず免責文を入れる(雛形に入っている)。

## ツールの位置づけ

**ツールが数字を出し、Claude が判断を助ける。** ツールは判断しない。
逆に Claude は数字を記憶や推測で作らない——必ずコマンドを叩いて、その出力を根拠にする。

```
python3 tool/kabu.py <コマンド>
```

相談を始めるときは、まず `brief`(事実シート)を読む。

## 相談されたときの手順

1. **`kabu brief`** を実行して、口座・保有・出口・リスク・引っかかっているルールを把握する
2. 銘柄名だけ言われたら → **調べる手順**(下)へ
3. 「◯◯切られた」と言われたら → `kabu sweep <コード>` の4方向を実際に調べ、
   **「相場のせい / 銘柄のせい / 話が別」の3択**で返す
4. 数字の話になったら、該当コマンド(`ladder` / `risk` / `holdings`)を叩いて答える。
   暗算しない
5. 取引・逆指値の変更・調査が発生したら、**その場で記録する**(下)

## 調べる手順(銘柄名だけ言われたとき)

`kabu research <コード>` がチェックリストを出す。その順に実際に集める。

**絶対ルール(過去の失敗から)**

1. **ニュースサイトの要約だけで書かない。会社の一次資料(短信・説明資料)を WebFetch で読む。**
   質疑応答要旨があれば必ず。楽天で「営業利益331億」と書き、原本は営業"損失"331億だった事故がある
2. **会社発表と市場コンセンサスを必ず突き合わせる。** 会社資料は実績しか教えない。
   楽天の急落を「急騰の反動」と書いたが、主因はコンセンサス比28.8%未達だった
3. **増益率は中身を分解する。** `kabu decompose` を使い、一過性・市況・実質に分ける。
   受注高と営業CFも見る。IHIは営業利益732億のうち400億が不動産売却益だった
4. 数値が取れなければ**「取得不可」と明記**する。推測で埋めない
5. 訂正したら、レポートの更新履歴に**「何をどう間違えたか」**を残す

集め終わったら公開レポートへ:

```bash
python3 tool/kabu.py report new 6areas --name 銘柄名 --sector セクター --price 1,234 --verdict 中立
```

9つの節の TODO を一次資料から埋める → `kabu portal` → `kabu check` → `kabu publish`。

## 記録する

そのとき記録しないと、あとから正確には戻せない。

```bash
kabu prememo <コード> <価格>          # ⑧ 買う前メモ。買いの前に必ず
kabu buy  <コード> <株数> <価格> --frame long --stop 4000 --theme DC・AI
kabu sell <コード> <株数> <価格> --reason "..."
kabu stop <コード> <価格>              # 引き上げ。切り下げは拒否される
kabu price 7011=4256 3402=1355         # 大引け後の現在値
kabu note "タイトル" "本文"            # 日記に一行
kabu render diary                      # private/trade_diary.html を作り直す
```

スクショが来たら数字を照合して反映する。**約定履歴が最終的な正**
(保有画面の平均取得単価は丸めて表示されている)。

## 毎朝

```bash
kabu morning
```

逆指値の生存確認が1日の最初のタスク。**GMOクリック証券の逆指値は期限切れで消えることが
多発している。** ツールが見ているのは記録の方なので、**証券口座の「(注文中)」を目で見て
突き合わせる**こと。確認できたら `kabu morning --ok 7011 7974 ...` で記録する。

## 公開まで通しで

```bash
python3 tool/kabu.py portal        # index.html を生成(不整合があれば止まる)
python3 tool/kabu.py check         # プライバシー検査
python3 tool/kabu.py publish --push -m "レポートを更新"
```

`publish` は生成 → 検査 → commit → push を1本で通す。検査に落ちたら commit しない。

## HTML を作ったら

ライト/ダーク両方でブラウザ確認する(Playwright 等)。色は dataviz の検証済みパレット
(青 `#2a78d6` / 赤 `#e34948` 系)。共通スタイルは `tool/kabulib/theme.py` が唯一の定義で、
個別レポートは共通クラス(`.card` / `h2` / `table` / `.num` / `.up` / `.down` / `.warn` /
`.note` / `.tag` / `.box`)だけで書く。

独自 CSS が要るときは **`<body>` の中に `<style>` を置き、ラッパークラスで名前空間を切る**
(`.tmap` / `.chem` のように)。`kabu check` がこの規約を検査する。

ポータルは分割構成: `index.html` は一覧だけを持ち、タイルは各レポートの個別ページへの
リンクになる(旧 `#コード` リンクは index がリダイレクト)。個別ページの上部の
「← 一覧に戻る」は `theme.document(back="index.html")` が付ける——手で書かない。

## テスト

```bash
python3 -m unittest discover -s tool/tests
```

ルール計算の期待値は実際の日記の手計算と突き合わせてある。ルールの数値を変えるなら
`tool/kabulib/config.py` を直す(そこが唯一の定義)。

## 詳しく

`tool/README.md` にコマンド一覧とデータ構造。
運用の口調・ルールブックの原本は `CLAUDE.local.md` と `private/rulebook.md`(どちらも非公開)。
