# -*- coding: utf-8 -*-
"""パスとルール定数。ルール番号は trade_diary.html のルールブック(全19条)に対応する。

ここの数値がルールの唯一の定義。コードの他の場所にマジックナンバーを書かない。
"""
from pathlib import Path

# ── パス ────────────────────────────────────────────────
# tool/kabulib/config.py → tool/ → リポジトリルート
ROOT = Path(__file__).resolve().parent.parent.parent

PRIVATE = ROOT / "private"          # .gitignore 済み。ポジション情報はここから出さない
DATA = ROOT / "data"                # 公開メタデータ(レポート一覧など)
PUBLIC = ROOT                       # GitHub Pages が配信する場所

STORE_FILE = PRIVATE / "store.json"          # 保有・約定・日記・ウォッチ・口座
REPORTS_FILE = DATA / "reports.json"         # 公開レポートのメタ(旧 build_portal.py の META)
DIARY_OUT = PRIVATE / "trade_diary.html"
PERF_OUT = PRIVATE / "performance.html"
PORTAL_OUT = PUBLIC / "index.html"

# ── ルール定数 ──────────────────────────────────────────

# ④ サイズ(金額)— 損切りが置けない/不明確なときのデフォルト上限
R4_SWING_PROBE_MAX_YEN = 500_000     # スイングの打診買い
R4_LONG_ONE_SHOT = 0.07              # 長期枠の1回 = 総資産の7%
R4_SINGLE_NAME_MAX = 0.15            # 1銘柄の上限 = 総資産の15%(常に適用)

# ⑤ サイズ(リスク)— こちらが本命。1回で失う額を固定する
R5_RISK_PER_TRADE = 0.015            # 総資産の1.5%

# ⑥ テーマ集中 — 1テーマは総資産の3〜4割まで
R6_THEME_WARN = 0.30
R6_THEME_MAX = 0.40

# ③-2 触らない枠 — −50%になっても触らずにいられる額。目安は総資産の3%
R3_HANDS_OFF_MAX = 0.03

# ⑧ 買う前メモの有効期限 — 株価が5%動くか3営業日
R8_MEMO_MOVE = 0.05
R8_MEMO_DAYS = 3

# ⑩ トレーリング逆指値(5段ラダー)
#   第1段: 買った直後の損切り(直近安値の下、または −5%)
#   第2段以降: 含み益が閾値に達したら「取得 + 利益 × 分数」へ引き上げる
R10_INITIAL_STOP = 0.05              # 第1段のデフォルト(−5%)
R10_LADDER = [                       # (含み益の閾値, 利益に掛ける分数, 段の名前)
    (0.25, 0.75, "第5段 +25%(3/4)"),
    (0.15, 2 / 3, "第4段 +15%(2/3)"),
    (0.10, 0.50, "第3段 +10%(1/2)"),
    (0.05, 0.00, "第2段 +5%(建値)"),
]
R10_BAND = (0.04, 0.10)              # 現在値から何%下に置くか。この帯を守る

# ⑫ 総リスク上限 — 全逆指値が同時発動したときの合計損失
R12_TOTAL_RISK_MAX = 0.06            # 総資産の6%

# ⑮ ピラミッディング
R15_MIN_GAIN = 0.05                  # 第1回が +5% 以上乗ってから
R15_MAX_ADDS = 2                     # 追加は上限2回

# ── 枠 ──────────────────────────────────────────────────
# 枠は「持つ期間」ではなく「出口の決め方」の区分
FRAMES = {
    "long": "長期枠",
    "swing": "スイング枠",
    "hands_off": "触らない枠",
}
# 触らない枠は逆指値を置かない(撤退は論理で決める)ので、⑩⑫の対象外
NO_STOP_FRAMES = {"hands_off"}

# ── 色(dataviz 検証済みパレット)──────────────────────
PALETTE = {
    "light": {"pos": "#2a78d6", "neg": "#e34948", "alt": "#eb6834", "aqua": "#1baf7a"},
    "dark": {"pos": "#3987e5", "neg": "#e66767", "alt": "#d95926", "aqua": "#199e70"},
}

# ── 公開レポートの免責(必ず末尾に入れる)───────────────
DISCLAIMER = (
    "※本レポートは公開情報の整理であり、投資勧誘・投資助言を目的とするものではありません。"
    "投資判断はご自身の責任で行ってください。"
)
